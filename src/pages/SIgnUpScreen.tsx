import SignUpComponent from "@/components/SignUpComponent";

const SignUpScreen = () => {
  // const { isLoading } = useValidateUsers("/homepage");

  return <SignUpComponent componentModel='SignUp' />;
};

export default SignUpScreen;
